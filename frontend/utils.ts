/*
 * Images are now served from public/images.
 * The path is already complete in content.json (e.g., "/images/hero_bg.jpeg")
 */
export const imageUrl = (path: string) => path;

/*
 * Audio files are served from public/audio.
 */
export const audioUrl = (path: string) => path;
